{
  "name": "frontend-app",
  "version": "1.0.0",
  "private": true,
  "main": "expo-router/entry",
  "scripts": {
    "start": "expo start",
    "reset-project": "node ./scripts/reset-project.js",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "lint": "expo lint && prettier --write ."
  },
  "dependencies": {
    "@trip-check/types": "1.0.0",
    "@trip-check/utils": "1.0.0"
  }
}
